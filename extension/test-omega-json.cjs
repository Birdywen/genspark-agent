const fs=require('fs');const vm=require('vm');const assert=require('assert');const source=fs.readFileSync(__dirname+'/content-src/01-omega-json.js','utf8');vm.runInThisContext(source);const LF=String.fromCharCode(10);const slash=String.fromCharCode(92);const valid=JSON.stringify({tool:'run_process',params:{stdin:'line1'+LF+'line2'}});const invalid=valid.replace('line1'+slash+'nline2','line1'+LF+'line2');assert.equal(parseOmegaPayload(invalid).params.stdin,'line1'+LF+'line2');const raw=JSON.stringify({tool:'run_process',params:{stdin:{$raw:'patch'}}})+LF+'ΩRAW patch'+LF+'print("hello")'+LF+'中文'+LF+'ΩRAWEND patch';assert.equal(parseOmegaPayload(raw).params.stdin,'print("hello")'+LF+'中文');assert.throws(()=>parseOmegaPayload(JSON.stringify({tool:'run_process',params:{stdin:{$raw:'missing'}}})),/not found/);assert.throws(()=>parseOmegaPayload('{"tool":"run_process","params":{"stdin":"unterminated}}'),/JSON|position|line/i);assert.throws(()=>parseOmegaPayload(JSON.stringify({tool:'x',params:{stdin:{$raw:'p'}}})+LF+'ΩRAW p'+LF+'x'),/Unclosed/);console.log('omega json parser tests passed');
